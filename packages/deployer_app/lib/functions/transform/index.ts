import { StateTransformerHandler } from '@thantos/merge_deploy';
import { TestManifestV1 } from '../merge';

export const handler: StateTransformerHandler<TestManifestV1> = () => {
  // TODO do something like generate a s3
};
